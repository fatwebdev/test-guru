<% if entity.errors.present? %>
  <div>
    <b><%= pluralize(entity.errors.count, 'Error') %> detected:</b>
    <ul>
      <% entity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>