<h1><%= t('.header') %></h1>

<table>
  <thead>
    <tr>
      <th><%= t('.table.title') %></th>
      <th><%= t('.table.questions_count') %></th>
      <th><%= t('.table.level') %></th>
      <th colspan="3"><%= t('.table.actions') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render @tests %>
  </tbody>
<table>

<br/>
<%= link_to t('.new_test'), new_admin_test_path %>