<tr>
  <td><%= question.body %></td>
  <td><%= link_to 'show', admin_question_path(question) %></td>
  <td><%= link_to 'edit', edit_admin_question_path(question) %></td>
  <td><%= link_to 'delete', admin_question_path(question), method: :delete, data: { confirmation: "Are you sure?" } %></td>
</tr>