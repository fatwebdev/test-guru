<%= form_with model: model, local: true do |f| %>
  <%= render 'shared/errors', entity: entity %>

  <%= f.label :body %><br/>
  <%= f.text_area :body, id: :question_body %>

  <p>
    <%= f.submit %>
  </p>
<% end %>